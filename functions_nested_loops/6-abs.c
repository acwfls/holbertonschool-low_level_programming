#include "main.h"

/**
* _abs(int) - Enter point
*
* Description: computes the absolute value of an integer.
* @i: absolute value being computed
*
* Return: 0 or 1 or -1
*/

int _abs(int i)

{
	if (i > 0)
	{
	return (i);
	}
	else if (i < 0)
	{
	return (-i);
	}
	else
	{
	return (0);
	}
}
